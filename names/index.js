const newArray = require("../country/state/city/index.js");

const newFunction = require("../utilities/utils/index.js");

const getPeopleInCity = (newArray) => {
  return newFunction(newArray);
};
module.exports = getPeopleInCity;
